export default class Weapon {
  constructor(name, type, pointsForNextSkill, points) {
    this.name = name
    this.type = type
    this.pointsForNextSkill = pointsForNextSkill
    this.points = points
  }
}